import React from 'react';

import loadfd from '../../../assets/dd/loadfd.gif';

let Preloader = (props) => {
    return  (
    <div>
    <img src={loadfd} />
    </div>
    )
}

export default Preloader;